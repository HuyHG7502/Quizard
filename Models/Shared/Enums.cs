namespace Quizard.Models.Shared
{
    public enum QuestionOrderDirection
    {
        Up = -1,
        Down = 1
    }

    public enum QuizAttemptStatus
    {
        InProgress,
        Completed
    }
}
